/**
*
* TestButton
*
*/

import React from 'react';

import styles from './styles.css';

class TestButton extends React.Component { // eslint-disable-line react/prefer-stateless-function
  render() {
    return (
      <div className={styles.testButton}>
      </div>
    );
  }
}

export default TestButton;
